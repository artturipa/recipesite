const mycv = () => {
  // Jumbotron pic, name, quote

  // sidepanels header, contentJSON
  //   where content JSON takes in array of obj, between objs there will be a gap, and each obj has text and header specified

  //
  console.log("IN MY CV");
  return (
    <div>
      <h1>Nothing here yet =)</h1>
    </div>
  );
};

export default mycv;
